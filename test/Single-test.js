import { expect } from 'chai';



describe('Process Model', () => {
    let model;

    before(() => model = {});

    after(() => model = undefined);


    it('process', (done) => {
      // Call done() if successful
      done("Please Implement"); 
    });

});